{
  "name": "spotify-downloader",
  "version": "1.0.0",
  "description": "Download songs and playlists from Spotify",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "render-build": "npm install && cd client && npm install @babel/plugin-proposal-private-property-in-object && npm install && npm run build"
  },
  "dependencies": {
    "archiver": "^5.3.1",
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "react-helmet-async": "^2.0.5",
    "spotify-web-api-node": "^5.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
